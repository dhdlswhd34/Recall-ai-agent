version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - meeting_data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  meeting_data:
    driver: local
